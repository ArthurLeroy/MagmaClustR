% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate-data.R
\name{generate_prior_means}
\alias{generate_prior_means}
\title{Generate the prior mean of the mean process, for every output}
\usage{
generate_prior_means(
  grid_list,
  prior_means = NULL,
  m0_intercept = c(-50, 50),
  m0_slope = c(-5, 5)
)
}
\arguments{
\item{grid_list}{A list of grid objects (one per output), as returned by
\code{build_output_grid_list()}.}

\item{prior_means}{An optional vector or list, providing one element per
output (a scalar, or a vector matching the output's grid length).}

\item{m0_intercept}{A vector of 2 numbers. Interval for the intercept of
the default linear trend.}

\item{m0_slope}{A vector of 2 numbers. Interval for the slope of the
default linear trend.}
}
\value{
A list of length \code{length(grid_list)}, each element being the prior
mean (scalar or vector) associated with the corresponding output.
}
\description{
If \code{prior_means} is provided, it is used as-is (one scalar, or one full
vector matching an output's grid length, per output). Otherwise, a linear
trend (randomly drawn intercept and slope, applied along the first
coordinate of each output's grid) is generated for every output, providing
a single, output-count-agnostic default.
}
\keyword{internal}
