% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.to_legacy_id}
\alias{.to_legacy_id}
\title{Normalise a task-level tibble's identifier column back to 'ID'}
\usage{
.to_legacy_id(df)
}
\arguments{
\item{df}{A tibble or data.frame.}
}
\value{
\code{df}, with 'Task_ID' renamed to 'ID' if present; unchanged
otherwise.
}
\description{
Inverse of the 'ID' -> 'Task_ID' renaming performed when converting
outputs to the new format (see \code{\link[=format_to_new]{format_to_new()}}). Used internally by
functions that \emph{recompute} on hyper-parameters/data pulled from a
\code{trained_model} (e.g. \code{\link[=hyperposterior]{hyperposterior()}}, \code{\link[=pred_magma]{pred_magma()}}), so that
the deep computational machinery (joins by 'ID', etc.) never needs to be
aware of which convention the object is currently in.
}
\keyword{internal}
