% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preconditioning.R
\name{precondition_hp}
\alias{precondition_hp}
\title{Precondition hyper-parameters via a pooled-likelihood pre-fit}
\usage{
precondition_hp(data, kern, precondition_tasks = 1, noise = TRUE)
}
\arguments{
\item{data}{A tibble, in the internal working format (column 'ID',
'Input', 'Output', and, for the multi-output case, 'Output_ID').}

\item{kern}{A kernel, character string or \code{\link[=convolution_kernel]{convolution_kernel()}}.}

\item{precondition_tasks}{A number in (0, 1], the fraction of tasks used
for the preconditioning fit. Default 1 (all tasks). Values closer to 0
reduce the preprocessing cost, at the risk of a less representative fit.}

\item{noise}{A logical value, whether to draw/fit a 'noise'
hyper-parameter.}
}
\value{
A condensed hyper-parameter tibble (no 'ID' column): the result of
\code{\link[=train_shared_gp]{train_shared_gp()}} fitted on the (sub)sampled tasks.
}
\description{
Computes a data-driven, reasonably-good starting point for the
hyper-parameters used to initialise the EM/VEM algorithms in
\code{\link[=train_magma]{train_magma()}} and \code{\link[=train_magmaclust]{train_magmaclust()}} (an 'iteration 0'). Rather than a
purely random draw, (a subset of) the tasks in 'data' are used to fit a
single Gaussian Process with hyper-parameters shared across tasks (via
\code{\link[=train_shared_gp]{train_shared_gp()}}), pooling information across tasks to stabilise the
subsequent EM/VEM optimisation and reduce the risk of pathological
training runs.
}
\keyword{internal}
