% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.build_grid_inputs}
\alias{.build_grid_inputs}
\title{Build the prediction grid of inputs (single/multi-output, single/multi-input)}
\usage{
.build_grid_inputs(grid_inputs, data, names_col, fn_name = "pred_magma")
}
\arguments{
\item{grid_inputs}{NULL, a numeric vector, or a data frame (new front-end
format).}

\item{data}{A tibble in the internal working format (already converted by
\code{\link[=resolve_mo_mode]{resolve_mo_mode()}}), used both to define the default grid ranges and, in
multi-output mode, the set of observed outputs.}

\item{names_col}{A character vector of the input column names of the working
format (coordinate dimensions, plus 'Output_ID' in multi-output mode).}

\item{fn_name}{A character string naming the calling function, used only to
craft error messages.}
}
\value{
A tibble with a 'Reference' column followed by the \code{names_col}
columns, sorted by 'Reference'.
}
\description{
Centralises the resolution of the user-facing \code{grid_inputs} argument
shared by \code{\link[=pred_gp]{pred_gp()}}, \code{\link[=pred_magma]{pred_magma()}}, \code{\link[=pred_magmaclust]{pred_magmaclust()}},
\code{\link[=hyperposterior]{hyperposterior()}} and \code{\link[=hyperposterior_clust]{hyperposterior_clust()}}. It turns \code{grid_inputs}
(NULL, a numeric vector, or a data frame in the new front-end format) into a
tibble of prediction inputs expressed in the internal working convention,
with a 'Reference' key built exactly like the one attached to the (already
converted) \code{data}.
}
\details{
Resolution rules:
\itemize{
\item \code{NULL}: an automatic regular grid is built on the numeric coordinate
dimension(s), spanning the observed range of each in \code{data}.
\item a numeric vector: interpreted as the grid of 'Input' values; only valid
when the data have a single coordinate dimension.
\item a data frame: read from the new front-end format. If an 'Input_ID' column
is present, its (sorted, integer) levels are mapped to 'Input', 'Input2',
... and the coordinate grid is the Cartesian product of the distinct
'Input' values per level. If 'Input_ID' is absent, the data frame is
assumed to already use the working/legacy coordinate columns (e.g. 'Input',
or 'Input' plus a named covariate). Any other column is ignored.
}

In multi-output mode, the coordinate grid is replicated across the
'Output_ID' values supplied in \code{grid_inputs} if present, otherwise
across every output observed in \code{data}.
}
\keyword{internal}
